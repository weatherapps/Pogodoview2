<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pogoda.com.pogodoview" >
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".PogodaView"
            android:label="Pogodoview" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity android:name=".Lokalizacja" android:parentActivityName=".PogodaView">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".PogodaView" />
    </activity>
    <activity
        android:name=".Powitanie"
        android:label="@string/title_activity_prognoza_biezaca">

    </activity>
        <activity
            android:name=".PogodaViewPrzy"
            android:label="pogoda przeszła">

        </activity>
</application>


    </manifest>
